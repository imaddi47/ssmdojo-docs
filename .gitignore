# Secrets — never commit
.env
.env.*
*.key
*.pem

# Local, per-developer harness settings (commit the shared configs, not local overrides)
.claude/settings.local.json
.codex/config.local.toml

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Dependency / build artifacts
node_modules/
dist/
build/

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/
