## v0.2.0 — 2026-07-01

### Features

- **desktop:** refresh entitlement on window focus so a purchase reflects without re-login
- **app:** add "Export all" button to the backup screen
- **app:** selective backup/restore UI with exists badges + skip/overwrite
- **app:** backup api contract for selective export + dedup import
- **server:** selective backup export + skip/overwrite dedup import
- **server:** startup migration backfilling tunnel uuids + re-keying rdp secrets
- **core:** configStore.replaceAllTunnels for atomic full-list rewrite
- **core:** selective export + uuid-matched dedup import (planImport)
- **core:** uuid validation + tunnel-id backfill helper
- **app:** backup & restore screen behind ProGate + nav
- **app:** show plan + upgrade in the account menu
- **app:** gate primitives (pro badge, upgrade prompt, pro gate)
- **app:** authState entitlement + useEntitlement hook
- **desktop:** resolve + carry entitlement in auth state; poll refresh
- **desktop:** entitlement cache + offline grace resolver
- **desktop:** cloud-client.fetchEntitlement (free on 401/404, throws on unreachable)
- **app:** backup export/import data layer
- **server:** backup export/import routes
- **core:** pure export/import of connections + settings (no secrets)
- **app:** billing menu item opening cloud billing page
- **app:** security menu item opening cloud 2fa management
- **app:** mount authprovider + account chip in the sidebar
- **app:** account chip (sign-in popover / profile + sign out)
- **app:** authProvider + useAuth (desktop bridge, inert on web)
- **app:** auth state types + window.ssm.auth typing
- **desktop:** register ssmdojo:// protocol + auth IPC wiring
- **desktop:** auth service orchestration + state machine
- **desktop:** keychain-backed auth token store
- **desktop:** cloud identity service client
- **desktop:** parse ssmdojo:// auth callback deep link
- **desktop:** auth config + PKCE primitives

### Bug Fixes

- **app:** disable restore on empty selection; drop redundant switch labels
- **app:** render ProBadge in the upgrade prompt
- **server:** validate tunnels + settings on backup import
- **desktop:** buffer cold-start deep link on win/linux; generalize cipher error
