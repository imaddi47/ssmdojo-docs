## v0.3.4 — 2026-07-16

### Features

- copy parameter version value + isolate side-by-side diff selection (#84)
- parameters editor drawer on narrow widths (#83)
- native file/folder Browse button on path inputs (#82)
- **settings:** default profile/region + configurable SSH keys folder (#81)

### Bug Fixes

- pick up new AWS credentials without a restart (+ list error state, e2e repair) (#86)
- diff selection (real fix), settings redesign, collapsed sidebar alignment (#85)
- recover from blank-screen crashes (error boundary + WS reconnect + Electron auto-reload) (#80)
- **app:** reload refreshes all AWS data with a minimum spin (#79)
