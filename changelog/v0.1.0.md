## v0.1.0 — 2026-06-15

### Features

- drop the circle ring, enlarge the SSM Dojo logo (#42)
- macOS auto-update + dual CI release pipelines (SP1) (#41)
- UX polish — floating combobox, custom checkboxes, clear buttons, copy-path (#40)
- **instances:** All-EC2 scan, ssh/tunnel badges, filter + Create SSH connection (#37)
- **files:** transfers over direct SSH + ssh/tunnel badges in the picker (#36)
- **ssh:** Connect/Disconnect + live traffic metering for SSH machines (#35)
- **ssh:** direct SSH connections (kind discriminator, SSH section, instance-ID resolve) (#34)
- **security:** lock the local server to the app (token on every route + session injection) (#33)
- **rdp:** filter, in-place start, stored encrypted credentials + cleaner client-missing error (#32)
- **tunnel form:** dropdown-with-custom-input for profile, region, instance, key (#30)
- **files:** sudo file transfers — stream download/upload over SSH (#28)
- **files:** sudo file operations — control ops (list/delete/du) (#26)
- **ssh:** per-tunnel SSH connection pool (responsive during transfers) (#24)
- **files:** delete, sticky bar, transfer history, overwrite confirm + sonner (#22)
- tunnels redesign + real traffic metering (#20)
- launch splash animation + rebrand to SSM Dojo (#19)
- profile/region reload, default profile, instances refresh + skeleton (#18)
- create a tunnel straight from a discovered instance (#17)
- gate the local API with a per-launch token (#16)
- RDP surface — mark a tunnel, connect via the native client (Spec 4) (#13)
- file explorer — Finder-style browse + scp/rsync transfers over SSH tunnels (Spec 3) (#11)
- **app:** close a terminal session when its tunnel stops
- **app:** add a persistent per-tunnel terminal session registry
- **app:** prompt for an ssh key passphrase in the terminal
- **server:** prompt for an ssh key passphrase and retry the connect
- **core:** add PassphraseRequiredError for encrypted SSH keys
- **app:** ssh form fields, terminal tab, and auto-start on open
- **app:** add the xterm ssh terminal pane
- **app:** add terminal controller wiring ws frames to a terminal
- **server:** add the ssh terminal websocket bridge
- **core:** add the real ssh2-backed connector
- **core:** add ssh connector interface and an echo connector
- **core:** resolve ssh auth from key path, agent, or default keys
- **core:** add host-key trust-on-first-use comparison
- **core:** add SSH config to the tunnel model
- **app:** only offer free-port when the holder is actually killable
- **server:** include holder killability in port-conflict details
- **core:** determine port-holder killability via process.kill(pid,0)
- **app:** informed-consent port-conflict prompt (service, cwd, PIDs, warning)
- **app:** surface structured error details + freePort result in the API client
- **server:** include conflicting port holders (with cwd) in 409 details
- **core:** enrich port holders with working directory (lsof -d cwd)
- **core:** add waitForPortFree polling helper
- **app:** clear a tunnel's logs when a fresh session starts
- **server:** restart a running tunnel when its config is edited
- **app:** prompt to free or change a busy local port on start
- **server:** pre-flight local port on start and add free-port route
- **core:** detect and free processes holding a local port
- **app:** edit tunnels in place inside the card (accordion)
- **ui:** support controlled card expansion
- **app:** richer tunnel cards with chips, stats, inline actions, and edit
- **app:** add useUpdateTunnel mutation
- **ui:** expand card on whole-row click, chevron keeps keyboard control
- **desktop:** add electron main, preload, and app-dir resolver
- **server:** optionally serve the built ui with spa fallback
- **app:** wire live tunnel status, logs, and start/stop/retry
- **server:** wire session manager events to the bus
- **server:** add tunnel lifecycle routes and runtime status
- **core:** export session engine api
- **core:** add session manager and default launcher
- **core:** add tunnel session state machine
- **core:** add local port availability probe
- **core:** add session types and aws arg builder
- **app:** make region picker type-to-filter
- **app:** support remote-host tunnels with labeled ports
- **server:** expand region list to all aws partitions
- **app:** add region selector and guard empty profile/region
- **app:** add tunnels screen with create and inline detail
- **app:** add instances screen
- **app:** add settings screen
- **app:** add sidebar, top bar, and app shell
- **app:** add api context, ui store, and query hooks
- **app:** add typed api client
- **server:** add localhost serve entrypoint
- **ui:** export public design-system api
- **ui:** add props grid and stat card
- **ui:** add expandable card and tabs
- **ui:** add banner, search input, and empty state
- **ui:** add button, status led, chip, and badge
- **ui:** add theme controller with match-system support
- **ui:** add design tokens, tailwind preset, and cn helper
- **server:** add runtime wiring and public exports
- **server:** add websocket event bridge
- **server:** add tunnel definition crud routes
- **server:** add system, aws, and settings routes
- **server:** add request validation
- **server:** add http error type and mapper
- **server:** add server event bus
- **server:** add server types and region list
- **core:** export public engine api
- **core:** add ssm/ec2 instance discovery
- **core:** add credential validation and error classification
- **core:** add aws profile listing
- **core:** add prerequisite detection
- **core:** add config store for settings and tunnels
- **core:** add atomic json file io
- **core:** add shared domain types
- **core:** scaffold @ssm/core with vitest smoke test

### Bug Fixes

- **desktop:** default-import electron-updater so the packaged app launches (#47)
- **release:** emit only the version on bump-version.sh stdout (#45)
- **release:** commit+tag the bump and import the signing cert in CI (#44)
- **server:** run the overwrite upload pre-delete under sudo + map its errors (#29)
- **app:** stop terminal clipping its last row (#25)
- **core:** raise SSH handshake timeout for high-latency tunnels (#27)
- **desktop:** startup-failure dialog and navigation guard (#14)
- **server:** reject foreign-origin websocket connections (CSWSH) (#12)
- stop orphaning tunnel processes on quit; reuse the server on dock reopen (#9)
- **desktop:** make the packaged electron app serve the ui (#7)
- **app:** restore xterm stylesheet import — fixes blank terminal
- **app:** open the terminal on the next frame to survive StrictMode
- **app:** optimistically show Starting for instant tunnel-start feedback
- **app:** defer terminal open until attached to fix glitchy render
- **core:** serialize ConfigStore writes to prevent a lost-update race
- **app:** keep the ssh terminal session alive across navigation
- **app:** give the terminal tab a real stopped state with retry
- **core:** report the real ssh exit code on session close
- **app:** re-open the conflict prompt when kill & free leaves the port held
- **server:** poll lsof holders when waiting for a local port to free
- **server:** gate tunnel start on lsof port holders (catch any-address binds)
- **app:** route detail Start through the conflict-aware handler
- **server:** wait for the local port to free before starting a tunnel
- **core:** start/restart sessions with the latest tunnel definition
- **core:** kill the session process group so stop frees the local port
- **server:** preserve port 0 so desktop gets an os-assigned free port
- **app:** select per-tunnel logs with a stable empty fallback
- **core:** guard double-start and resolve stop after exit in tunnel-session
- **app:** validate tunnel port inputs as numbers
- **app:** avoid exactOptionalPropertyTypes error in InstancesScreen query params
- **ui:** a11y — label search input, status role for info banner, tab aria linkage
- **ui:** default button type and make card expand label state-aware
- **ui:** guard applyTheme for ssr and remove dark-default token ambiguity
- **ui:** make react and tailwind peer deps; back cn with tailwind-merge
- **server:** reject non-string free-text settings and cover invalid mode
- **server:** use app error schema for unknown routes and cover instances no-search
- **core:** drop id-less instances and cover ssm pagination
